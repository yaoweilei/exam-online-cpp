# Exam Online 功能设计书

版本：v0.2  
日期：2026-06-18  
适用范围：当前 `exam-online-cpp` 代码库，面向 JLPT / EJU 在线学习、个人自学、培训机构教学管理与运营后台。

## 1. 产品定位

Exam Online 是一个面向日语考试学习的在线刷题与教学管理系统。

核心目标：

- 为个人用户提供完整的自学闭环：选卷、做题、听力、解析、错题、复习、报告。
- 为培训机构提供教学闭环：学习组、班级、约课、学员、老师、作业、成绩册、学员档案、机构看板。
- 为平台运营提供基础后台：内容管理、用户管理、付费权益、审计日志、功能开关。

当前系统已经具备本地和内测环境可用的主要功能。生产上线前仍需要补齐真实第三方服务接入、支付验签、短信费用风控、生产通知系统和内容质检流程。

## 2. 状态定义

| 状态 | 含义 |
|---|---|
| 已闭环 | 前端入口、后端 API、数据读写、基础测试已经打通，本地/内测可完整使用 |
| 基础可用 | 主要链路已实现，但上线依赖外部服务、商户配置或进一步风控 |
| 待新开发 | 目标模型已经确定，但当前代码还没有按该模型完整实现 |
| 待生产接入 | 当前是 mock / stub / 本地实现，需要接真实 provider |
| 待优化 | 功能存在，但体验、权限粒度、测试覆盖或数据质量仍需加强 |

## 3. 用户、角色与权限设计

### 3.1 设计原则

这是新系统设计，角色体系不需要迁就任何历史字段或临时角色名。角色只回答“这个人是谁”，权限回答“这个人能做什么”，作用域回答“能在哪些数据范围内做”。

核心原则：

- 固定少量基础角色，避免把每个现实岗位都做成系统角色。
- 现实岗位通过“基础角色 + 权限模板 + 额外权限 + 作用域”表达。
- 页面和 API 不直接依赖岗位名称，应判断权限点和数据作用域。
- 学生不绑定单一班级，老师也不绑定单一班级，所有教学关系都通过 `learningGroup` 和 `enrollment` 表达。
- 平台角色和机构角色隔离，机构管理员不能分配平台级角色。

权限计算规则：

```text
最终权限 = 基础角色默认权限 + 权限模板权限 + 额外授权 - 禁用权限 + 数据作用域限制
```

同一权限同时存在 `allow` 和 `deny` 时，`deny` 优先。

### 3.2 基础角色模型

系统固定 6 个正式角色，`guest` 只表示未登录状态，不算机构或平台内部角色。

| 角色 | 名称 | 主要对象 | 说明 |
|---|---|---|---|
| `student` | 学员/个人用户 | 个人用户、机构学员 | 做题、看解析、复习、提交作业、查看个人报告 |
| `assistant` | 教学运营人员 | 助教、班主任、教务、课程顾问 | 催交、跟进、约课协助、学员档案维护、课程包辅助管理 |
| `teacher` | 老师 | 任课老师、约课老师 | 负责教学、作业、批改、课后反馈、备课和弱项跟进 |
| `orgAdmin` | 机构管理员 | 机构负责人、校区负责人、教务主管 | 管理机构成员、学习组、课程包、套餐席位、机构看板和审计 |
| `contentAdmin` | 内容管理员 | 平台内容维护人员 | 管理试卷、图片、音频、答案、解析、发布状态 |
| `superAdmin` | 平台超级管理员 | 平台运营/技术管理员 | 拥有全平台管理权限 |

登录状态单独处理：

| 状态 | 名称 | 说明 |
|---|---|---|
| `guest` | 访客 | 未登录用户，可浏览公开内容、试看、进入登录注册 |

### 3.2.1 初始用户与种子账号

初始用户分成两类：生产初始化账号和开发/演示种子账号。生产环境不能直接带一批可登录的固定账号，否则会留下安全风险；开发环境需要保留各角色账号，方便权限、页面和 E2E 测试。

生产环境第一版只内置一个平台初始化账号：

| 用户ID | 显示名 | 角色 | 作用域 | 创建方式 | 登录策略 |
|---|---|---|---|---|---|
| `platform_owner` | 平台所有者 | `superAdmin` | `platform` | 首次部署 setup 流程创建 | 必须设置强密码，首次登录强制改密，建议开启 MFA |

生产环境规则：

1. `platform_owner` 只能在系统未初始化时创建一次。
2. `platform_owner` 创建后写入审计日志，不能通过普通注册产生。
3. 不预置 `student`、`teacher`、`assistant`、`orgAdmin`、`contentAdmin` 可登录账号。
4. 机构管理员由平台后台创建机构时邀请或创建，不作为全局默认账号。
5. 老师、助教、学员由机构邀请、批量导入、手机号登录后绑定机构关系产生。
6. 内容管理员由 `superAdmin` 在平台后台创建，并必须记录授权原因。
7. 任何生产账号都不能使用固定默认密码，不能使用 `dev-empty`、mock OAuth、debug code 登录。

开发/演示环境允许生成这些种子账号：

| 用户ID | 显示名 | 角色 | 作用域 | 用途 |
|---|---|---|---|---|
| `superAdmin01` | 超级管理员01 | `superAdmin` | `platform` | 测试平台后台、功能开关、全站统计、审计 |
| `superAdmin02` | 超级管理员02 | `superAdmin` | `platform` | 测试多管理员和审计差异 |
| `contentAdmin01` | 内容管理员01 | `contentAdmin` | `platform` | 测试试卷、音频、图片、答案解析维护 |
| `contentAdmin02` | 内容管理员02 | `contentAdmin` | `platform` | 测试内容审核、发布和双人协作 |
| `orgAdmin01` | 机构管理员01 | `orgAdmin` | `organization` | 测试机构、成员、学习组、课程包、套餐席位 |
| `orgAdmin02` | 机构管理员02 | `orgAdmin` | `organization` | 测试同机构或多机构管理员场景 |
| `teacher01` | 教师01 | `teacher` | `learningGroup` | 测试布置作业、成绩册、学员档案、备课 |
| `teacher02` | 教师02 | `teacher` | `learningGroup` | 测试多老师协作和学习组可见范围 |
| `assistant01` | 助理01 | `assistant` | `learningGroup` / `campus` | 测试催交、跟进、课程包查看、学员备注 |
| `assistant02` | 助理02 | `assistant` | `learningGroup` / `campus` | 测试不同权限模板和额外授权 |
| `student01` | 学生01 | `student` | `personal` / `learningGroup` | 测试个人自学、作业提交、错题、报告 |
| `student02` | 学生02 | `student` | `personal` / `learningGroup` | 测试多学员、排名、提交情况和机构档案 |

开发/演示种子账号规则：

1. 只在 `APP_ENV=development`、`test` 或本地 demo 初始化时生成。
2. 种子账号必须带 `seed_user=true` 或等价标记，便于重置和清理。
3. 种子账号密码可以由本地脚本生成，但不能写明文默认密码到仓库。
4. E2E 临时用户继续使用唯一前缀，例如 `student_teaching_*`、`orgadmin_teaching_*`，不算初始用户。
5. `guest` 不是数据库用户，只是未登录状态；如果为了兼容测试生成 `guest` 记录，也不能拥有超过 `exam.public.view` 的权限。
6. 开发种子账号可以绑定示例机构、学习组、课程包和作业，但这些演示数据必须和生产初始化数据隔离。

第一版推荐初始化流程：

```text
未初始化系统
  -> 创建 platform_owner
  -> platform_owner 创建内容管理员或机构
  -> 机构管理员邀请老师 / 助教 / 学员
  -> 用户通过邮箱、手机号、微信或用户名密码完成登录和绑定
```

### 3.3 权限模板

`assistant` 覆盖的现实岗位较宽，产品上应提供权限模板，让机构选择“这个人主要做什么”。模板只是预设权限集合，保存后仍然落到具体权限点，不产生新的基础角色。

| 模板 | 基础角色 | 默认适用岗位 | 默认权限重点 |
|---|---|---|---|
| 助教模板 | `assistant` | 助教 | 催交作业、查看提交、写学习备注、协助老师反馈 |
| 班主任模板 | `assistant` | 班主任、学管 | 查看学员档案、编辑跟进记录、查看续费风险、安排建议作业 |
| 教务模板 | `assistant` | 教务 | 管理约课、课程包、学习组、排课和课时扣减 |
| 课程顾问模板 | `assistant` | 顾问、销售 | 查看基础档案、编辑跟进记录、查看续费风险，不默认查看完整成绩细节 |
| 校区管理员模板 | `orgAdmin` | 校区负责人 | 管理指定校区内的成员、学习组、课程包和看板 |

### 3.4 现实岗位映射

现实岗位不等于系统角色。岗位名称可以在机构成员资料中显示，但权限判断只看基础角色、权限模板、额外权限和作用域。

| 现实岗位 | 推荐基础角色 | 推荐模板/额外权限 | 说明 |
|---|---|---|---|
| 学员 | `student` | 无 | 可以同时参加班级制学习组和约课制学习组 |
| 任课老师 | `teacher` | `lesson_prep.export`、`learning_record.feedback.edit` | 负责教学、作业、反馈和课前准备 |
| 助教 | `assistant` | 助教模板 | 协助老师催交、查看提交、写备注 |
| 班主任 / 学管 | `assistant` | 班主任模板 | 负责学习状态、跟进记录和续费风险 |
| 教务 | `assistant` | 教务模板 | 负责排课、课程包、学习组和课时 |
| 课程顾问 / 销售 | `assistant` | 课程顾问模板 | 负责线索、跟进和续费，不默认查看完整教学明细 |
| 校区负责人 | `orgAdmin` | 校区管理员模板，scope=`campus` | 管理某个校区 |
| 机构老板 / 机构管理员 | `orgAdmin` | scope=`organization` | 管理全机构 |
| 财务 | `assistant` 或 `orgAdmin` | `organization.billing.manage`、`payment.refund` | 初期不单独设角色，需要时用权限模板表达 |
| 内容教研 | `contentAdmin` | `content.publish` | 维护平台题库和解析质量 |
| 平台运营 / 技术管理员 | `superAdmin` | `*` | 平台最高权限 |

### 3.5 作用域层级

作用域决定权限能作用到哪些数据。建议从第一版就预留 `campus`，小机构可以不启用，多校区机构可以直接使用。

```text
personal < learningGroup < campus < organization < platform
```

| 范围 | 含义 |
|---|---|
| `personal` | 只能访问自己的数据 |
| `learningGroup` | 只能访问指定学习组，学习组可以是班级或约课课次 |
| `campus` | 只能访问指定校区 |
| `organization` | 只能访问本机构 |
| `platform` | 全平台 |

作用域规则：

1. 更高作用域不自动授予更高风险权限，只扩大已有权限的数据范围。
2. 机构成员默认最高只能到 `organization`，不能到 `platform`。
3. 学生默认只有 `personal` 和自己加入的 `learningGroup`。
4. 老师和助教默认只有自己负责或参与的 `learningGroup`。
5. 校区负责人使用 `campus`，机构负责人使用 `organization`。

### 3.6 学习关系规则

学生和老师都不直接绑定固定班级。系统用 `learningGroup` 表达教学关系，用 `enrollment` 表达用户在学习组中的身份。

学生规则：

- 一个学生可以同时加入多个 `learningGroup`。
- 一个学生可以同时有班级制学习、约课制学习和个人自学任务。
- 一个学生可以拥有多个 `coursePackage`，课程包不绑定固定教师。
- 学生端展示按“任务、课程、反馈、课时余额”组织，不按班级制/约课制拆入口。

教师规则：

- 老师不绑定单一班级，只通过 `enrollment(role=teacher)` 获得某个学习组的教学权限。
- 老师可以同时负责长期班级、短期小班、一对一约课。
- 老师能看到的学员、成绩、作业和反馈，由其参与的 `learningGroup` 和额外授权决定。
- 老师端展示按“今日安排、负责学习组、待批改、待反馈、待跟进学生”组织。

权限判断不写死“某老师是否是某班班主任”，而是判断：

```text
用户角色 + 用户在 learningGroup 中的 enrollment 身份 + 权限点 + 作用域
```

### 3.7 谁可以给谁授权

授权能力必须明确，否则机构规模变大后权限会失控。

| 授权人 | 可以管理 | 不能做 |
|---|---|---|
| `superAdmin` | 平台全部用户、机构、内容、支付、功能开关 | 不应参与机构日常教学管理 |
| `orgAdmin` | 本机构内 `student`、`assistant`、`teacher`、`orgAdmin` 的成员权限 | 不能授予 `contentAdmin`、`superAdmin`、`platform` 作用域 |
| `teacher` | 自己负责学习组内的作业、反馈、建议任务 | 不能修改成员角色和机构权限 |
| `assistant` | 按授权范围跟进学员、催交、约课、维护档案 | 默认不能给别人授权 |
| `contentAdmin` | 平台内容发布和质检权限 | 不能管理机构成员、课程包、支付和学习数据 |
| `student` | 自己的账号资料和学习数据 | 不能授权别人 |

通用授权规则：

1. 任何人不能授予自己没有的权限。
2. 任何人不能把别人提升到高于自己的作用域。
3. `superAdmin`、`contentAdmin` 只能由平台后台分配。
4. `deny` 可用于临时关闭某个默认能力，例如机构管理员只允许看看板但不允许管理账单。
5. 所有授权、禁用、过期、撤销都必须写入审计日志。

### 3.8 平台角色与机构角色隔离

机构后台只展示机构角色：

- 学员
- 助教/教学运营
- 老师
- 机构管理员

平台后台才展示平台角色：

- 内容管理员
- 平台超级管理员

隔离规则：

- 机构管理员不能看到 `contentAdmin`、`superAdmin` 的分配入口。
- 内容管理员不能进入机构成员权限页面。
- 平台超级管理员可以查看和修正机构数据，但常规机构运营不依赖 `superAdmin`。
- 内容数据的发布链路和机构教学链路分开审计。

### 3.9 默认工作台

每个角色登录后应该看到不同的主入口，避免所有用户共用同一个复杂后台。

| 角色 | 默认工作台 | 重点信息 |
|---|---|---|
| `student` | 学习工作台 | 今日复习、每日一练、待完成作业、近期课程、错题变化、剩余课时/套餐 |
| `assistant` | 跟进工作台 | 待催交、待约课、今日跟进、老师备注、学员状态、课程包余量 |
| `teacher` | 教学工作台 | 今日课程、负责学习组、待批改、待反馈、弱项排行、备课入口 |
| `orgAdmin` | 机构管理工作台 | 学习组、成员权限、课程包、排课日历、机构看板、席位、审计 |
| `contentAdmin` | 内容工作台 | 试卷内容、音频/图片、答案解析、质检状态、发布记录 |
| `superAdmin` | 平台运营工作台 | 用户、机构、支付、功能开关、审计日志、数据修复 |

### 3.10 默认权限建议

| 角色 | 默认权限 |
|---|---|
| `student` | `exam.practice`、`answer.submit`、`analysis.view`、`assignment.submit`、`report.view.self`、`learning_record.view.self` |
| `assistant` | `assignment.review`、`assignment.remind`、`student.profile.view`、`student.profile.edit`、`student.followup.edit`、`learning_record.comment`、`course_package.view` |
| `teacher` | `assignment.create`、`assignment.review`、`assignment.remind`、`gradebook.view`、`student.profile.view`、`student.profile.edit`、`lesson_prep.create`、`lesson_prep.export`、`learning_record.feedback.edit` |
| `orgAdmin` | `organization.member.manage`、`learning_group.manage`、`course_package.manage`、`lesson.booking.manage`、`organization.dashboard.view`、`organization.billing.manage`、`audit.view` |
| `contentAdmin` | `content.exam.edit`、`content.audio.manage`、`content.image.manage`、`content.answer.edit`、`content.analysis.edit`、`content.publish`、`content.audit.view` |
| `superAdmin` | `*` |

推荐权限格式：

```text
resource.action
```

### 3.11 成员权限覆盖

机构后台提供“成员权限”页面，用于处理某个成员需要临时或长期增加、禁用某些权限的情况。

成员权限页面包含：

- 基础角色：学员 / 助教或教学运营 / 老师 / 机构管理员。
- 权限模板：助教、班主任、教务、课程顾问、校区管理员。
- 额外权限：用 checkbox 单独开关。
- 权限效果：`allow` 或 `deny`。
- 权限范围：个人 / 学习组 / 校区 / 机构。
- 权限有效期：永久 / 到某个日期。
- 操作记录：谁给谁加了什么权限、何时修改、修改前后内容。

权限覆盖结构：

```json
{
  "permission": "student.import",
  "effect": "allow",
  "scope": "organization",
  "scope_id": "org_default",
  "expires_at": "2026-12-31T23:59:59Z"
}
```

推荐先支持这些机构常用权限点：

| 权限点 | 含义 |
|---|---|
| `assignment.create` | 布置作业 |
| `assignment.review` | 查看/批改提交 |
| `assignment.remind` | 催交作业 |
| `gradebook.view` | 查看成绩册 |
| `student.profile.view` | 查看学员档案 |
| `student.profile.edit` | 编辑学员档案 |
| `student.followup.edit` | 编辑学员跟进记录 |
| `student.import` | 批量导入学员 |
| `learning_group.manage` | 管理学习组 |
| `lesson.booking.manage` | 管理约课 |
| `course_package.manage` | 管理课程包 |
| `course_package.view` | 查看课程包 |
| `lesson_prep.create` | 备课组卷 |
| `lesson_prep.export` | 导出讲义 |
| `renewal_risk.view` | 查看续费风险 |
| `organization.dashboard.view` | 查看机构看板 |
| `organization.member.manage` | 管理机构成员 |
| `organization.billing.manage` | 管理套餐和席位 |
| `payment.refund` | 发起退款 |
| `audit.view` | 查看审计日志 |

### 3.12 数据可见范围

数据可见范围比角色名称更重要。所有页面和 API 都应按下表判断可见性。

| 数据 | 学员 | 老师 | 助教/教学运营 | 机构管理员 | 内容管理员 | 平台超级管理员 |
|---|---|---|---|---|---|---|
| 自己的学习记录 | 可见 | 仅自己作为学员时可见 | 仅自己作为学员时可见 | 仅自己作为学员时可见 | 仅自己作为学员时可见 | 可见 |
| 所属学习组作业 | 可提交 | 可布置/批改 | 可查看/催交/备注 | 可查看/管理 | 不可见 | 可见 |
| 所属学习组成绩 | 可看自己 | 可看负责学习组 | 按授权查看 | 可看授权范围 | 不可见 | 可见 |
| 学员档案 | 可看自己 | 可看负责学习组学员 | 按授权查看/编辑 | 可看机构范围 | 不可见 | 可见 |
| 续费风险 | 不可见 | 按授权查看 | 按授权查看 | 可见 | 不可见 | 可见 |
| 课程包和课时 | 可看自己 | 不默认可见 | 按授权查看/管理 | 可管理 | 不可见 | 可见 |
| 支付订单 | 可看自己的 | 不可见 | 按授权查看 | 可看机构订单 | 不可见 | 可见 |
| 题库和解析内容 | 可学习使用 | 可教学使用 | 可教学使用 | 可教学使用 | 可编辑/发布 | 可管理 |
| 机构成员权限 | 不可见 | 不可见 | 不可见 | 可管理本机构 | 不可见 | 可管理 |
| 审计日志 | 不可见 | 不可见 | 不可见 | 按授权查看 | 内容审计可见 | 可见 |

## 4. 账号系统

### 4.1 功能范围

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 用户名/密码登录 | 已闭环 | 支持常规账号和开发测试账号 |
| 注册 | 已闭环 | 前端入口、密码校验、注册后自动登录 |
| 退出 | 已闭环 | 前端清理本地 session，后端注销 token |
| session 恢复 | 已闭环 | 浏览器刷新后通过 token 恢复 `/me/context` |
| 游客/登录态区分 | 已闭环 | 默认 guest，登录后注入用户上下文 |
| 手机号验证码登录 | 基础可用 | 支持验证码、自动创建账号、每日发送限制 |
| 微信登录 | 基础可用 | 开发 mock 可用，真实开放平台待配置 |
| OAuth 登录 | 基础可用 | mock 回调可用，真实 provider 待接入 |
| 修改密码 | 已闭环 | 个人中心入口可用 |
| 忘记密码 | 已闭环 | 邮件/短信验证码链路可用，开发环境暴露 debug code |

### 4.2 关键流程

登录流程：

1. 用户打开登录弹窗。
2. 选择微信、手机号、密码或 OAuth。
3. 后端创建 session token。
4. 前端调用 `/api/v1/me/context` 获取用户、角色、权限、会员和机构上下文。
5. 前端保存 `exam_v2_token` 和 `exam_v2_user`。
6. 应用根据用户状态刷新个人中心、功能开关和可见入口。

退出流程：

1. 用户点击退出。
2. 前端调用 `/api/v1/auth/logout`。
3. 后端删除内存 session 和持久化 session。
4. 前端清空本地 token/user。
5. 前端恢复为 guest。

### 4.3 测试覆盖

Playwright 专项命令：

```powershell
npx playwright test tests/e2e/auth-session.spec.js --ui --ui-host 127.0.0.1 --ui-port 9323
```

已覆盖：

- 游客状态
- 密码登录
- 刷新恢复 session
- 退出后旧 token 失效
- 注册后自动登录
- 手机号验证码登录
- 错误密码不建立 session
- 忘记密码
- 修改密码
- 微信 mock 登录
- OAuth mock 登录
- student / teacher / orgAdmin / superAdmin 权限识别

## 5. 试卷学习核心

### 5.1 功能范围

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 考试类型选择 | 已闭环 | JLPT / EJU |
| 年份/场次/级别选择 | 已闭环 | 基于本地 JSON 索引加载 |
| 正常显示题目 | 已闭环 | 支持题干、选项、图片 |
| 正常作答 | 已闭环 | 支持单选和答题卡跳题 |
| 保存答案 | 已闭环 | 用户答案可保存 |
| 显示答案 | 已闭环 | 答案区已接入 |
| 答案解析 | 已闭环 | 支持“答案解析” |
| 补充解析 | 已闭环 | 支持“补充解析” |
| 假名/中文辅助 | 已可用 | 已优化作文/读解间距 |
| 做题进度记录 | 已闭环 | 支持恢复进度 |

### 5.2 EJU 特殊处理

EJU 日语包含：

- 记述
- 読解
- 読聴解
- 聴解

当前已处理的问题：

- EJU 答题卡去掉无意义 `Section` 文案。
- 听力题解析中去掉“原文版式”图片块。
- 读听解图片与题号错位问题已修正过。
- 2024 / 2025 音频已按 EJU 模式切割，且对真题音频加了边界缓冲，避免句尾漏词。

### 5.3 内容数据要求

每份试卷应满足：

- 题干正确。
- 选项正确。
- 图片正确。
- 答案正确。
- 音频对应正确。
- 解析达到自学可用水平。
- 假名和中文翻译位置舒适。
- JSON 数据结构稳定。

## 6. 听力核心

### 6.1 功能范围

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 音频播放 | 已闭环 | 单题音频播放 |
| 暂停/继续 | 已闭环 | 基础播放器能力 |
| 单题音频对应 | 已闭环 | EJU 2024/2025 已重点校正 |
| 逐句播放 | 已闭环 | 依赖 transcript 时间戳 |
| transcript 显示 | 已闭环 | 支持听力原文展示 |
| 读听解图片/题干/音频对应 | 已闭环 | 已做多轮修正 |

### 6.2 音频切割规范

真题 EJU 采用 37-track 结构：

- `track_01` - `track_05`：说明/练习/开始提示
- `track_06` - `track_17`：读听解 1-12
- `track_18` - `track_21`：听解说明/练习/开始提示
- `track_22` - `track_36`：听解 13-27
- `track_37`：结束语

模拟题如果原始音频直接从第 1 题开始，则采用 27-track 结构：

- `track_01` - `track_27`：第 1-27 题

切割要求：

- 不截掉题号开头。
- 结尾保留足够缓冲，避免漏掉句尾词。
- 特殊题目若“一段音频对应多个答题编号”，允许复制同一音频段给多个 track。
- 如果题册中题目缺失，使用静音占位保留编号对齐。
- 每个切割目录需要生成 `split_manifest.json`。

## 7. 自学闭环

### 7.1 功能范围

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 错题本 | 已闭环 | 支持错题记录与回看 |
| 收藏夹 | 已闭环 | 支持单题收藏、原因、分类 |
| 生词本 | 已可用 | 支持词汇记录 |
| 今日复习 | 已闭环 | 聚合 SRS、错题、每日一练 |
| 每日一练 | 已可用 | 支持日常练习入口 |
| 学习报告 | 已可用 | 展示学习数据和趋势 |
| 备考目标 | 已可用 | 支持目标配置 |
| 连续学习/今日目标 | 已可用 | 支持 streak 和目标进度 |
| 弱项分析 | 已可用 | 基于题型和错题统计 |
| 推荐复习 | 已闭环 | 推荐原因、去练习、完成反馈 |

### 7.2 推荐复习逻辑

推荐复习应综合：

- 错题频率
- 最近练习时间
- 题型弱项
- SRS 到期项
- 当前备考目标
- 每日目标完成情况

前端展示必须包含：

- 推荐原因
- 进入练习
- 完成后的反馈

## 8. 个人账户与付费权益

### 8.1 个人账户

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 个人信息 | 已可用 | 个人中心可查看和编辑 |
| 邮箱/手机号绑定 | 已可用 | 支持验证码验证 |
| 联系方式变更验证 | 已可用 | 变更前需挑战验证 |
| 推荐码 | 已可用 | 支持推荐奖励 |
| 数据导出 | 已闭环 | 支持个人数据导出 |
| 多端同步 | 基础可用 | 状态可拉取，冲突解决仍可加强 |

### 8.2 付费权益

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 个人套餐 | 已可用 | 支持套餐与权益判断 |
| 续费/升级入口 | 已可用 | 前端入口存在 |
| 兑换码 | 已闭环 | 支持兑换和权益发放 |
| 卡券包 | 已可用 | 支持卡券查看 |
| 支付订单 | 基础可用 | 订单模型和接口已存在 |
| 支付流水 | 已可用 | 支持 ledger |
| 权益判断 | 已可用 | 订阅与 entitlements |
| 到期/续费状态 | 已可用 | 支持状态展示 |

### 8.3 生产缺口

真实支付仍需按实际商户接入：

- 微信支付下单
- 支付宝下单
- Stripe Checkout
- Webhook 验签
- 退款
- 对账
- 风险控制

## 9. 机构用户闭环

### 9.0 精简支持班级制与约课制的统一模型

系统需要同时支持两种培训机构形态：

- 班级制：学生加入长期班级，老师按班级布置作业、看成绩册。
- 约课制：学生购买课程包，每次约课生成一次短周期教学关系，上一次课扣一次课时。

为了保持系统精简，不建议做两套独立系统。推荐统一抽象为：

```text
organization
  ├─ members
  ├─ campuses
  ├─ coursePackages
  ├─ learningGroups
  ├─ learningTasks
  └─ learningRecords
```

核心原则：

- 用户不绑定模式，一个学生可以同时参加班级学习和约课学习。
- 机构可以有默认模式，但不能限制用户只能使用一种模式。
- 小机构可以不启用校区；多校区机构用 `campus` 控制成员、学习组、课程包和看板范围。
- 每个 `learningGroup` 自己决定是班级还是约课。
- 作业、成绩、反馈、权限都挂到 `learningGroup` 或 `student`。
- 课程包不绑定固定教师，只绑定学生、科目、课时余额和有效期。

#### 9.0.1 learningGroup

`learningGroup` 是统一教学关系容器。

班级制中：

```text
长期班级 = learningGroup(type=class)
```

约课制中：

```text
一次课 / 小班课 / 一对一课 = learningGroup(type=booking)
```

示例：

```json
{
  "id": "lg_japanese_class_001",
  "organization_id": "org_001",
  "campus_id": "campus_tokyo_001",
  "type": "class",
  "subject": "japanese",
  "name": "EJU日语冲刺A班",
  "starts_at": "2026-07-01T19:00:00+08:00",
  "ends_at": "2026-09-30T21:00:00+08:00",
  "status": "active"
}
```

```json
{
  "id": "lg_sogo_booking_20260620",
  "organization_id": "org_001",
  "campus_id": "campus_tokyo_001",
  "type": "booking",
  "subject": "sogo",
  "name": "文综一对一 6月20日",
  "course_package_id": "pkg_sogo_20_001",
  "starts_at": "2026-06-20T19:00:00+08:00",
  "ends_at": "2026-06-20T20:30:00+08:00",
  "status": "scheduled"
}
```

一个学生可以同时存在于多个学习组：

```text
王同学
  ├─ EJU日语冲刺A班          learningGroup(type=class, subject=japanese)
  ├─ 文综一对一 6月20日课     learningGroup(type=booking, subject=sogo)
  └─ 记述写作小班             learningGroup(type=class, subject=writing)
```

#### 9.0.2 enrollment

`enrollment` 表示某个用户在某个学习组中的身份。

```json
{
  "group_id": "lg_sogo_booking_20260620",
  "user_id": "teacher_002",
  "role": "teacher"
}
```

```json
{
  "group_id": "lg_sogo_booking_20260620",
  "user_id": "student_001",
  "role": "student"
}
```

权限判断不再写死“老师是否是班级老师”，而是判断：

```text
老师和学生是否同属一个 learningGroup
老师在该 learningGroup 中是否是 teacher / assistant
该 learningGroup 是否允许访问对应作业、成绩、档案或反馈
```

#### 9.0.3 coursePackage

课程包用于约课制，但不绑定固定教师。

课程包绑定：

- 机构
- 学员
- 科目
- 总课时
- 剩余课时
- 有效期
- 状态

课程包不绑定：

- 固定老师
- 固定课次

示例：

```json
{
  "course_package_id": "pkg_sogo_20_001",
  "organization_id": "org_001",
  "student_id": "student_001",
  "subject": "sogo",
  "title": "EJU文综 20课时包",
  "total_lessons": 20,
  "remaining_lessons": 8,
  "used_lessons": 12,
  "expires_at": "2026-09-30",
  "status": "active"
}
```

扣课时规则：

- 创建预约不立即扣课时，只锁定课时。
- 课次状态变成 `completed` 后扣 1 次课时。
- 课前取消释放锁定课时。
- 旷课是否扣课时由机构配置决定。
- 调课不重复扣课时，只迁移原课次时间。
- 小班约课中，每个学生各自从自己的课程包扣 1 次。

建议课次状态：

| 状态 | 含义 | 课时处理 |
|---|---|---|
| `scheduled` | 已预约/已排课 | 锁定 1 次课时 |
| `cancelled` | 已取消 | 释放锁定课时 |
| `rescheduled` | 已调课 | 不扣课时，关联新时间 |
| `completed` | 已上课 | 扣 1 次课时 |
| `no_show` | 学员未到 | 按机构规则扣或不扣 |

#### 9.0.4 learningTask

作业、课前任务、课后任务统一为 `learningTask`。

班级作业：

```json
{
  "id": "task_001",
  "target_type": "learningGroup",
  "target_id": "lg_japanese_class_001",
  "subject": "japanese",
  "title": "2023年第2回 EJU日语读解 1-10题",
  "due_at": "2026-07-10T23:59:59+08:00"
}
```

约课课后任务：

```json
{
  "id": "task_002",
  "target_type": "learningGroup",
  "target_id": "lg_sogo_booking_20260620",
  "subject": "sogo",
  "title": "课后整理日本近现代史时间线",
  "due_at": "2026-06-21T23:59:59+08:00"
}
```

个人补充任务：

```json
{
  "id": "task_003",
  "target_type": "student",
  "target_id": "student_001",
  "subject": "japanese",
  "title": "复习听读解图表题 10 道"
}
```

#### 9.0.5 learningRecord

`learningRecord` 记录学习结果和教学反馈。

可记录：

- 作业提交状态
- 自动批改结果
- 老师批改意见
- 课后反馈
- 老师备注
- 学员弱项
- 建议下一步任务

班级制中，成绩册按 `learningGroup(type=class)` 聚合。

约课制中，课后反馈和课后作业按 `learningGroup(type=booking)` 聚合。

#### 9.0.6 学生和老师视角

学生端不按“班级制/约课制”分入口，而是按任务和安排展示：

- 今日任务
- 近期课程
- 我的学习项目
- 我的作业
- 我的反馈
- 剩余课时和到期提醒

老师端也不按模式分入口，而是按工作流展示：

- 今日安排
- 我负责的学习组
- 待批改
- 待反馈
- 待跟进学生
- 备课入口

#### 9.0.7 新开发落地方式

第一版直接以 `learningGroup` 为机构教学的一等对象，以 `campus` 作为可选组织范围。班级、约课课次、小班课都不再单独设计成彼此割裂的模型。

建议核心数据对象：

| 对象 | 说明 |
|---|---|
| `organizations` | 机构 |
| `campuses` | 可选校区，用于 50-100 人机构的数据范围管理 |
| `memberships` | 机构成员和机构级角色 |
| `course_packages` | 学员课程包，不绑定固定教师 |
| `learning_groups` | 班级或约课课次 |
| `enrollments` | 用户在学习组里的身份 |
| `learning_tasks` | 作业、课前任务、课后任务 |
| `learning_records` | 提交、成绩、反馈、备注 |

任务第一版就使用统一目标字段：

```json
{
  "target_type": "learningGroup",
  "target_id": "lg_001"
}
```

班级制不单独建独立班级表，而是使用学习组类型：

```json
{
  "type": "class"
}
```

约课制也不单独建 `lesson_sessions` 表，而是使用：

```json
{
  "type": "booking"
}
```

校区不改变教学模型，只作为数据范围：

```json
{
  "campus_id": "campus_tokyo_001",
  "organization_id": "org_001",
  "name": "东京校区"
}
```

### 9.1 机构与学习组管理

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 创建机构 | 已闭环 | 支持组织创建 |
| 机构套餐/席位 | 已可用 | 支持席位与套餐状态 |
| 校区管理 | 基础可用 | 小机构可关闭，多校区机构用于数据范围；机构后台已有基础管理页 |
| 学习组管理 | 已闭环 | 统一支持 `learningGroup(type=class/booking)`；班级、小班和约课都通过学习组表达 |
| 约课管理 | 已可用 | 基于 `learningGroup(type=booking)`；完成约课时会扣减绑定课程包 1 次，机构后台已有排课日历 |
| 课程包管理 | 已可用 | 不绑定教师；机构后台可维护课时包，约课完成后自动扣课时，并在教学工作台显示余量预警 |
| 添加学员 | 已闭环 | 支持成员管理 |
| 移除学员 | 已闭环 | 支持成员移除 |
| 老师/助教/学员权限 | 已闭环 | 基于基础角色、权限模板、额外权限、作用域、有效期和组织审计 |
| 机构邀请码/邀请链接 | 已闭环 | 支持邀请展示与接受 |

### 9.2 作业系统

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 老师布置作业 | 已闭环 | 当前支持指定试卷/题目范围 |
| 指定学习组 | 已闭环 | 作业绑定 `organization_id + learning_group_id`，支持 `learningGroup(type=class/booking)` |
| 学员查看作业 | 已闭环 | 学员侧入口 |
| 学员完成作业 | 已闭环 | 支持提交 |
| 自动批改 | 已可用 | 客观题自动评分 |
| 老师查看提交情况 | 已闭环 | 支持提交状态 |
| 催交入口 | 已可用 | 手动催交入口存在 |
| 定时催交 | 待优化 | 例如截止前 24 小时自动提醒 |

### 9.3 成绩与档案

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 学习组成绩册 | 已闭环 | 基于 `learningGroup` 聚合作业提交、平均分、学员弱项和个人档案入口 |
| 学员个人档案 | 已闭环 | 支持学习轨迹查看 |
| 错题变化 | 已可用 | 支持趋势数据 |
| 作文历史 | 已可用 | 支持历史记录 |
| 听力弱项 | 已可用 | 可按题型统计 |
| 老师备注 | 已闭环 | 支持备注 |
| 建议作业 | 已可用 | 可作为老师推荐任务 |

### 9.4 机构看板

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 学习组平均分趋势 | 已可用 | 基于学习组作业提交和平均分生成趋势 |
| 学员排名 | 已可用 | 基于机构/学习组可见学员的练习记录生成排名 |
| 各题型弱项 | 已可用 | 支持题型维度 |
| 老师教学效果 | 已可用 | 基础指标 |
| 续费风险学生 | 已可用 | 风险提示 |
| 席位使用情况 | 已可用 | 机构席位监控 |
| 教学工作台聚合 | 已闭环 | 返回老师/学员日程、课程包预警、学生学习关系和已保存备课方案 |

## 10. 内容管理

### 10.1 内容对象

系统需要管理以下内容对象：

- JLPT 试卷 JSON
- EJU 试卷 JSON
- 题目图片
- 听力音频
- transcript
- 答案
- 答案解析
- 补充解析
- 假名
- 中文翻译
- 作文题与参考答案

### 10.2 内容质量标准

解析需要达到“人类教师讲解、自学者可理解”的水平。

每道题解析建议包含：

- 题目考点
- 解题路径
- 正确答案为什么成立
- 错误选项为什么不选
- 听力题的关键听点
- 读解题的定位句
- 易错点
- 补充学习建议

### 10.3 内容发布流程建议

推荐流程：

1. 导入原始 PDF / 音频 / 答案。
2. 提取题干、选项、图片、音频。
3. 人工校对题干和选项。
4. 生成初版解析。
5. 按标准文档人工润色。
6. 自动内容审计。
7. 抽样人工复核。
8. 发布。

## 11. 老师备课工具

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 按考点组卷 | 已可用 | 可按题型/考点筛选 |
| 保存备课方案 | 已闭环 | 可把试卷、关键词、题目集、讲义配置保存到机构备课方案 |
| 复用讲义 | 已可用 | 已保存方案会出现在教学工作台，后续可继续扩展为编辑/复制 |
| 导出讲义 | 已可用 | 支持讲义接口和 HTML 下载 |
| 打印题目 | 已可用 | 支持隐藏答案场景 |
| 隐藏答案 | 已可用 | 适合课堂讲解 |
| 课堂投屏模式 | 已可用 | 适合老师上课展示 |
| 生成作业 | 已闭环 | 前端可把当前备课方案中的题目一键布置到选中的学习组 |

## 12. 社区

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 试卷讨论 | 已可用 | 题目/试卷维度讨论 |
| 发帖 | 已可用 | 支持发帖 |
| 评论 | 已可用 | 支持评论 |
| 点赞 | 已可用 | 支持点赞 |
| 删除/管理 | 已可用 | 管理能力需要结合权限 |

## 13. 运营后台

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 用户搜索 | 已可用 | 支持用户检索 |
| 角色权限 | 已可用 | 可查看和识别角色 |
| 功能开关 | 已可用 | 支持开关管理 |
| 审计日志 | 已可用 | 支持筛选、分页、CSV 导出 |
| 反馈处理 | 已可用 | 支持反馈数据 |
| 全站统计 | 已可用 | 支持基础统计 |

## 14. PWA 与体验增强

| 功能 | 当前状态 | 说明 |
|---|---|---|
| 安装应用 | 已可用 | 支持 PWA 安装入口 |
| service worker | 已可用 | 支持注册 |
| 移动端布局 | 已可用 | 仍需持续回归 |
| 缓存策略 | 已可用 | 需按生产资源策略细化 |

## 15. 数据与目录设计

### 15.1 主要目录

| 目录 | 用途 |
|---|---|
| `backend` | C++ Drogon 后端 |
| `frontend` | TypeScript 前端源码 |
| `static` | 静态资源和运行时页面 |
| `data/paper` | 试卷 JSON |
| `data/image` | 题目图片 |
| `data/audio` | 听力音频 |
| `data/user` | 用户、组织、学习数据 |
| `tests/e2e` | Playwright 自动化测试 |
| `tools` | 内容处理、审计、批处理工具 |

### 15.2 数据存储

当前系统主要使用 JSON 文件和 WAL 日志。

优点：

- 本地开发简单。
- 数据可直接检查。
- 便于内容批量处理。

生产风险：

- 并发写入能力有限。
- 大规模用户数据查询效率有限。
- 需要更严格的数据备份和恢复策略。

后续如用户量增加，建议将用户、订单、作业、提交、学习记录迁移到数据库。

## 16. API 设计原则

当前 API 已统一到：

```text
/api/v1
```

建议原则：

- 所有新接口继续使用 `/api/v1`。
- 用户态接口优先通过 token 获取当前用户。
- 管理接口必须校验权限点和 scope。
- 返回结构保持统一：`code`, `message`, `data`, `request_id`, `ts`。
- 高风险接口写审计日志。

## 17. 自动化测试设计

### 17.1 当前测试类型

| 类型 | 工具 | 用途 |
|---|---|---|
| 后端 smoke | CTest | 服务层和核心数据逻辑 |
| Web E2E | Playwright | 用户可见流程 |
| 内容审计 | Node tools | 试卷内容、音频、时间戳 |

### 17.2 关键测试命令

```powershell
npm run e2e:auto
npm run e2e:auto:headed
npm run e2e:auto:ui
npx playwright test tests/e2e/auth-session.spec.js --ui --ui-host 127.0.0.1 --ui-port 9323
ctest --test-dir backend/build -C Debug --output-on-failure
npm run audit:content:strict
```

### 17.3 必须持续覆盖的核心流程

- 游客访问
- 登录/退出/session 恢复
- 试卷选择
- 做题/保存/提交
- 答案/解析/补充解析
- 听力播放/逐句播放
- 错题/收藏/今日复习
- 个人套餐/兑换码/支付流水
- 机构班级/学习组/作业/成绩册/学员档案
- 角色权限边界

## 18. 当前上线前缺口

### 18.1 第三方登录

- 真实微信开放平台配置。
- 真实 OAuth code -> access token -> userinfo。
- 登录失败和绑定冲突处理。

### 18.2 短信与邮件

- 阿里云/腾讯云短信真实 provider。
- 发送频率、每日限制、IP 限制。
- 异常费用报警。
- 邮件送达率和退信处理。

### 18.3 支付

- 微信支付真实下单。
- 支付宝真实下单。
- Stripe Checkout。
- Webhook 验签。
- 退款和对账。
- 支付审计与风控。

### 18.4 内容质量

- 全量 EJU/JLPT 解析人工质检。
- 音频切割抽样试听。
- 图片裁切和题号对应复核。
- 中文翻译/假名显示回归。

### 18.5 机构通知

- 作业截止前自动催交。
- 站内信。
- 邮件通知。
- 短信通知。
- 微信服务通知。

## 19. 推荐迭代顺序

### 阶段 1：角色与权限统一

1. 已固定 6 个正式角色，`guest` 只作为未登录状态。
2. 已引入权限模板、额外授权、禁用权限和作用域字段。
3. 继续把页面和 API 从直接判断角色，逐步改为判断权限点和数据作用域。
4. 补齐成员权限页面的授权审计、有效期提示和边界测试。
5. 平台角色与机构角色继续保持隔离，机构管理员不能授予平台角色。

### 阶段 2：账号与付费生产化

1. 接真实短信 provider。
2. 接真实微信登录。
3. 接真实支付。
4. 完成 webhook 验签和退款对账。
5. 增加设备列表和主动踢下线。

### 阶段 3：内容质量生产化

1. 建立内容发布工作流。
2. 建立解析质量标准检查表。
3. 全量试卷内容审计。
4. 音频和 transcript 抽样回归。

### 阶段 4：机构教学闭环强化

1. 自动催交。
2. 续费风险通知。
3. 老师教学效果指标。
4. 批量导入学生。
5. 多校区和审计权限。

## 20. 验收标准

系统达到可正式上线前，至少需要满足：

- 账号系统 E2E 全通过。
- 核心做题流程 E2E 全通过。
- 机构作业闭环 E2E 全通过。
- 支付沙箱流程全通过。
- 内容审计 strict 通过。
- 角色权限边界测试覆盖 student / teacher / assistant / orgAdmin / contentAdmin / superAdmin。
- 生产配置不依赖 debug code。
- 日志和审计可追踪关键操作。
- 数据有备份与恢复方案。
